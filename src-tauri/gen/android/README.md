# Tauri Android Legacy

This directory is generated by Tauri Mobile and is kept only for legacy or
experimental debugging.

The official Nalu Android client lives in:

```text
mobile/android
```

Do not add new Nalu Mobile product code here. The native Android app shares
contracts with the desktop/server code through `shared/contracts`, not Vue UI,
Tauri IPC, or Desktop Rust Core.
